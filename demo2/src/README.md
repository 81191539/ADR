# demo2/src

This directory contains the C++ solver implementation snapshot for demo2.

It includes the demo entry point, runtime orchestration, solver logic, CPU/CUDA backends, I/O, and checkpoint handling used by this workspace.
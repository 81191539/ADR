# tools

This directory contains project-maintenance helper scripts.

- `check_encoding.ps1`: checks repository text encoding and line-ending conventions.

Use this directory for development, validation, migration, and maintenance scripts. Do not store simulation outputs or build artifacts here.
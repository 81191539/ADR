# webui/static

This directory contains the main Web UI frontend assets.

- `index.html`: page structure.
- `styles.css`: interface styling.
- `app.js`: browser-side interaction logic and API calls.

These files are served directly by `webui/server.py`. After changing them, start the Web UI and check the page in a browser.
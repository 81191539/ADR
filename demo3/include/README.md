# demo3/include

This directory contains the C++ header snapshot used by demo3.

It mirrors the main include/ structure for the demo workspace, preserving the interfaces and parameter structures needed to reproduce or compare that demo.